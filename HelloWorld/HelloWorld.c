#include<stdio.h>                   //preprocessor derivative and <stdio.h> being the header file (standard input and output)
int main(){                         //Calling of main() funtion
    printf("HELLO WORLD \n");       //Displaying function printf that is in the files of of "main()"
    return 0;                       //return 0 is the only case in which main() runs
}                                   // "\n" = newline charater